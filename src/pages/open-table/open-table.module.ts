import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { OpenTablePage } from './open-table';

@NgModule({
  declarations: [
    OpenTablePage,
  ],
  imports: [
    IonicPageModule.forChild(OpenTablePage),
  ],
})
export class OpenTablePageModule {}
